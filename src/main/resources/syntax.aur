//For imports the file ending .aur can be left out or be used
//The suffix as can also be used to specify a variable name for the import
import "libs/lib.aur" as "test"

//constant objects
const constant = true
//variable objects
let variable = true

//Main method invoked in each program
fun main() {

}

fun add(x, y) {
    return x+y;
}